import Slider from "@/components/slider";
import Header from "@/components/ui/header";

const Home = () => {
  return (
    <section>
      <Header />
      <Slider />
    </section>
  );
};
export default Home;
